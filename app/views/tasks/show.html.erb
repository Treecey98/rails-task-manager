<h2><%= @task.title %></h2>

<p>Description: <%= @task.details %></p>

<%= link_to "Edit a task", edit_task_path(@task) %>
<br>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<br>
<%= link_to "Back to list of tasks", "/tasks" %>
